@model IEnumerable<Group_18_Final_Project.Models.BookReorder>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReorderQuantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReorderQuantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookReorderID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookReorderID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookReorderID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
